---
import BaseLayout from '../layouts/BaseLayout.astro';

import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
---

<BaseLayout title="Policies | Bhumika Sheth" description="Policies that we follow at Bhumika Sheth therapy session booking">
	<div class="stack gap-20">
		<main class="wrapper policies">
			<Hero
				title="Policies"
				tagline="Thanks for stopping by. Read below to learn about policies that we follow at Bhumika Sheth therapy session booking."
			>
				<img
					alt="Bhumika Sheth smiling in a red plaid shirt and tortoise shell glasses"
					width="480"
					height="620"
					src="/assets/bhumika_sheth.jpg"
				/>
			</Hero>

			<section>
				<h2 class="section-title">Terms of services and conditions</h2>
				<div class="content">
					<p>
						The terms of services and conditions contained here in shall apply to any person (“User”) using the services of "Bhumika Sheth" for making payments (Therapy Session Booking) through an online payment gateway service (Service) offered by "RazorPay". Each "User" is therefore deemed to have read and accepted these above mentioned terms of services and conditions.
					</p>
				</div>
			</section>
			<section>
				<h2 class="section-title">Pricing policy for all the services</h2>
				<div class="content">
					<p>60 minutes therapy session = 100 USD</p>
					<p>Slots can be booked via <a target="_blank" href="https://calendly.com/thebhumikasheth/60" class="social">Book Session</a> and then subsequently the payment can be made following booking the slot</p>
				</div>
			</section>
			<section>
				<h2 class="section-title">Refund policy for all the services</h2>
				<div class="content">
					<p>
						<ul>
							<li>Cancelations can be rescheduled or refunded</li>
							<li>Cancelations before 24 hours of the scheduled appointment time will be refunded with 3% deduction of processing fee within 7 working days
								<ul>
									<li>Refund for 60 minutes therapy session = 100 - 3(3%) = 97 USD</li>
								</ul>
							<li>Cancelations within 24 hours of the scheduled appointment time will not be refunded</li>
							<li>No show on the scheduled appointment time slot will not be refunded</li>
						</ul>
					</p>
				</div>
			</section>
		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.policies {
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
	}

	img {
		margin-top: 1.5rem;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	section {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		color: var(--gray-200);
	}

	.section-title {
		grid-column-start: 1;
		font-size: var(--text-xl);
		color: var(--gray-0);
	}

	.content {
		grid-column: 2 / 4;
	}

	.content :global(a) {
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.content :global(a:hover),
	.content :global(a:focus) {
		text-decoration-color: currentColor;
	}

	@media (min-width: 50em) {
		.policies {
			display: grid;
			grid-template-columns: 1fr 60% 1fr;
		}

		.policies > :global(:first-child) {
			grid-column-start: 2;
		}

		section {
			display: contents;
			font-size: var(--text-lg);
		}
	}
</style>
